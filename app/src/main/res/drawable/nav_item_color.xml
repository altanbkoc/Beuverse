<?xml version="1.0" encoding="utf-8"?>
<selector
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Seçili durum -->
    <item android:color="@color/off_white" android:state_checked="true" />
    <!-- Seçili olmayan durum -->
    <item android:color="@color/light_gray" android:state_checked="false" />
    <!-- ÖNEMLİ: Önizleme için default renk -->
    <item android:color="@color/light_gray" /> <!-- Fallback -->

</selector>